CREATE TABLE ESTOQUE(
CODIGO_PRODUTO INT NOT NULL PRIMARY KEY,
NOME_PRODUTO VARCHAR(40),
QUANTIDADE_ENTRADA INT,
QUANTIDADE_SAIDA INT,
TOTAL INT
)

SELECT*FROM ESTOQUE

INSERT INTO ESTOQUE( CODIGO_PRODUTO, NOME_PRODUTO, QUANTIDADE_ENTRADA, QUANTIDADE_SAIDA)
VALUES(1, 'LAPIS', 200, 180),
(2, 'CANETA', 187, 98),
(3, 'CADERNO', 59, 28),
(4, 'GIZ', 400, 5),
(5, 'PAPEL', 1230, 1190),
(6, 'CARTUCHO', 5, 5);

SELECT*FROM ESTOQUE

CREATE PROCEDURE AT_TOTAL
AS 
BEGIN
	UPDATE ESTOQUE
	SET TOTAL=QUANTIDADE_ENTRADA-QUANTIDADE_SAIDA
END

EXECUTE AT_TOTAL